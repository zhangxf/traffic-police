<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.trafficpolice.dao.VideoDao">
	<insert id="doInsert" useGeneratedKeys="true" keyProperty="id" parameterType="org.trafficpolice.po.Video">
		insert into tp_video(category_id, name, origin_name, introduction, duration, file_size, url, thumb_url, create_time, update_time) 
		  values(#{categoryId}, #{name}, #{originName}, #{introduction}, #{duration}, #{fileSize}, #{url}, #{thumbUrl}, #{createTime}, #{updateTime})
	</insert>
	<update id="doUpdate" parameterType="org.trafficpolice.po.Video">
		update tp_video
		<set>
			<if test="categoryId != null">
				category_id = #{categoryId},
			</if>
			<if test="name != null">
				name = #{name},
			</if>
			<if test="originName != null">
				origin_name = #{originName},
			</if>
			<if test="introduction != null">
				introduction = #{introduction},
			</if>
			<if test="duration != null">
				duration = #{duration},
			</if>
			<if test="fileSize != null">
				file_size = #{fileSize},
			</if>
			<if test="explains != null">
				explains = #{explains},
			</if>
			<if test="url != null">
				url = #{url},
			</if>
			<if test="thumbUrl != null">
				thumb_url = #{thumbUrl},
			</if>
			update_time = #{updateTime}
		</set>
		<where>
			<if test="id != null">
				id = #{id}
			</if>
		</where>
	</update>
	<delete id="doDelete">
		delete from tp_video where id = #{id}
	</delete>
	<select id="findAll" resultType="org.trafficpolice.po.Video">
		select * from tp_video order by create_time
	</select>
	<select id="findById" resultType="org.trafficpolice.po.Video">
		select * from tp_video where name = #{name}
	</select>
	<select id="findByCondition" parameterType="org.trafficpolice.dto.VideoQueryParamDTO" resultType="org.trafficpolice.po.Video">
		select * from tp_video
		<where>
			<if test="id != null">
				id = #{id}
			</if>
			<if test="name != null">
				and name like CONCAT('%',#{name},'%')
			</if>
			<if test="categoryId != null">
				and category_id = #{categoryId}
			</if>
			<if test="createTimeBegin != null ">
				and create_time >= #{createTimeBegin}
			</if>
			<if test="createTimeEnd != null ">
				<![CDATA[and create_time <= #{createTimeEnd}]]>
			</if>
			<if test="updateTimeBegin != null ">
				<![CDATA[and update_time >= #{updateTimeBegin}]]>
			</if>
			<if test="updateTimeEnd != null ">
				<![CDATA[and update_time >= #{createTimeBegin}]]>
			</if>
		</where>
		order by create_time desc
	</select>
</mapper>